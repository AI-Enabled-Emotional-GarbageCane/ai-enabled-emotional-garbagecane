# AI 情緒垃圾筒 — 實作前報告講稿

> 共 13 張投影片，建議總時長 12-15 分鐘。

---

## Slide 1 — 封面

大家好，我們這組的題目叫做「AI 情緒垃圾筒」。

簡單來說，這是一個會用組員自己的聲音吐槽你的智慧垃圾桶。如果你把垃圾丟錯，它會用我們錄好的語音即時 roast 你。

副標題寫的是「因為海龜每天都在被吸管攻擊」——這不只是搞笑，等一下會說明為什麼海龜是我們選擇這個題目的出發點。

我們是 3 人團隊，預計 8 週完成實作。

---

## Slide 2 — 問題背景

先看四個數字。

每年有大約 1,270 萬噸塑膠流入海洋。台灣的回收率表面上看是 55-58%，數字很漂亮，但後段 MRF（資源回收分選廠）的污染率高達 17-25%——也就是說，很多被丟進回收桶的東西其實是錯的，反而拖慢了整個回收流程。台灣每年用掉 30 億支塑膠吸管，海龜被吸管卡住的畫面大家都看過。

我們的核心洞察是：海洋塑膠問題的本質是高集中度、可以在源頭攔截的工程問題。與其在後端處理，不如在投入的瞬間就擋下來。而吸管和海龜作為高擴散性的符號，讓「情緒回饋」這條路線在傳播學上有天然的優勢。校園是很好的部署場景，因為使用者集中、重複使用率高。

---

## Slide 3 — 相關工作

目前市場上已經有幾個智慧垃圾桶的產品。

波蘭的 Bin-e 做自動辨識分流，準確率 92% 以上，有雲端後台。美國的 TrashBot 用機械手臂分流，90-95% 準確率，大螢幕顯示。加拿大的 Oscar Sort 用螢幕指示使用者該丟哪桶，分類率 96%。

但請注意它們的回饋方式——全部都是中性的、正向鼓勵的、指示性的。

這就是我們看到的缺口：沒有任何方案使用「幽默 roast」、「負向情緒 nudge」、或「同儕個人化語音」。辨識準確率這塊已經高度擁擠，大家都在 90-97% 之間，邊際效益很低。我們認為真正的空白在互動層和情緒層。

---

## Slide 4 — 我們的切入點

所以我們的設計有三個刻意的選擇。

第一，用「同儕的聲音」。我們會預錄組員自己的 roast 語音，讓垃圾桶說出來的話聽起來就是你認識的人在吐槽你。這種熟悉感在學術上叫做 parasocial interaction——比起通用的合成語音，聽到認識的人吐槽你，衝擊力和記憶點完全不同。

第二，用「幽默 Roast」，不是嚴肅警告。幽默能降低心理抗拒，提升傳播性和記憶點。被罵但好笑，比被嚴肅警告更讓人記得住。

第三，「即時觸發」，不是事後通知。全自動化——你一靠近就偵測，丟入就判定，不用按任何按鈕。

這三個設計選擇在現有商業競品和學術文獻中都是空白的。

---

## Slide 5 — 行為設計理論支撐

這不是在惡搞，我們有學術依據。

Fogg 的行為模型告訴我們，行為改變需要動機、能力和觸發同時出現。我們的即時語音回饋就是一個高效的 Trigger，在使用者丟錯的當下觸發。

Comber 和 Thieme 2013 年的研究指出，負向情緒——包含羞愧和不適感——是打破壞習慣的正當設計工具，特別是在垃圾分類這種已經變成自動化行為的場域。

Skurka 等人 2018 年的研究比較了幽默訴求和恐懼訴求，結論是幽默更能降低心理抗拒、提升參與度；恐懼訴求反而因為焦慮過高而降低行為意圖。

最後，Berengueres 2013 年的 Emoticon-Bin 實驗證實：加入聲音和表情的遊戲化垃圾桶，回收率可以提升 3 倍。我們做的，是把這套設計本土化、人格化。

---

## Slide 6 — 系統架構（概覽）

系統分成三個模組：Firmware、Vision、Display，全部同機運行在一台 Jetson AGX Orin Nano 上，透過 Python 的 multiprocessing.Queue 溝通。

Firmware 負責用 L515 的 Depth 串流做距離感測和 LED 燈號控制。Vision 負責用 L515 的 RGB 串流做 YOLOv11n 推論和 Binary 分類。Display 負責狀態機、Roast 語音播放、Admin 面板和事件紀錄。

整個流程是：人靠近 → L515 depth 偵測 → 攝影機捕捉 → AI 判定 → 自動播語音 → 紀錄事件。全程零按鈕、零蓋子、零人工介入。

---

## Slide 7 — 系統架構圖

這張是完整的架構圖。

最上面是 Intel RealSense L515，它是一顆 RGB-D 鏡頭，同時輸出 RGB 影像和 Depth 深度資訊。Depth 資料往下送給 Firmware 模組做距離感測，RGB 資料往下送給 Vision 模組做模型推論。

三個模組都跑在 Jetson 裡面，各自是獨立的 Python Process。Firmware 透過 q_detected 這個 Queue 通知 Vision「有人靠近了」，Vision 做完推論後透過 q_result 把結果送給 Display。

下方是輸出設備：LED 燈號由 Firmware 控制，顯示 accept 或 reject；Speaker 和螢幕由 Display 控制，播語音和更新畫面。

整套架構同機運行，用 multiprocessing.Queue 做 IPC，不需要網路、不需要 MQTT，由一個 Launcher script 統一啟動三個 Process。

---

## Slide 8 — AI 模型與硬體選型

左邊是 AI 模型的部分。我們用 YOLOv11n 做 Binary 分類——只分 accept（一般垃圾）和 reject（資源回收物）。訓練資料是 TrashNet 加上 RealWaste，總共約七千多張圖，用 Google Colab 的 T4 GPU 訓練，再轉成 ONNX 和 TensorRT 部署到 Jetson。

特別的規則有兩條：偵測到超過一個物件就直接判定 reject，信心值低於 0.5 就播自嘲語音，不做判定。

右邊是硬體。運算核心是 Jetson AGX Orin Nano，感測用 Intel RealSense L515。L515 的 RGB 給 Vision 做推論，Depth 給 Firmware 做距離感測。三個模組之間用 multiprocessing.Queue 同機通訊。

---

## Slide 9 — 互動設計：Roast 語音分級

這是我們的 Roast 分級表。

丟對了，輕度肯定——「算你會。」不會太熱情，保持反差感。

丟錯了，中度吐槽——「這是塑膠不是紙，你眼睛被蛤蜊夾到了？」

如果同一個人 30 秒內連續丟錯兩次，升級到重度 Roast——「我已經第二次告訴你了，要不要去眼科？」

一次丟一堆進來的，中重度吐槽——「一次丟一堆是在趕投胎？分一下好嗎。」

如果模型信心值太低看不出來，就自嘲——「我看不太出來欸，可能是我老花。」

每一類準備 3-5 句隨機抽選，避免重複。播放前會先放一個短音效加上 0.5 秒停頓，製造「法官敲槌前的停頓感」，讓 Roast 的衝擊力更強。每句控制在 8 秒以內。

---

## Slide 10 — Display 設計

Display 有兩個角色。

左邊是使用者端的公告螢幕。上面會顯示今日統計——正確投入幾次、被抓到亂丟幾次。還有一個虛擬海龜計數器，把累積的正確分類數轉成「救活了幾隻虛擬海龜」，呼應海洋保育的主題。下方是最近的犯案紀錄，用 L515 的快照加上時間和辨識結果，存成本機 JSON log 和照片，不上雲。

右邊是 Admin 控制面板，給開發者和管理者用。可以看 L515 的連線狀態和模型載入狀態、即時調整 Confidence 閾值和 Roast 音量、匯出事件 log 為 CSV。Demo 的時候可以用這個面板展示系統的後台管理能力。

---

## Slide 11 — 分工與時程

時程分成四個 Phase，總共 8 週。

Phase 1 用前兩週做設計收斂——簽收文件、確認硬體、跑 Baseline 測試。Phase 2 用第三到第四週各自做 PoC——Vision 做模型 fine-tune、Firmware 把感測鏈路打通、Display 做語音播放器。Phase 3 用第五到第六週做整合——把 Queue 串起來、部署到 Jetson、找同學來試丟做測試。Phase 4 用第七到第八週做期末報告、拍 Demo 影片、期末展演。

分工的部分，我們用 3 個獨立的 sub-repo，各自依中心 repo 的 v0.3 contract 開發。在 GitHub 上可以清楚看到每個人維護的項目：vision repo 負責影像辨識——TrashNet 加 RealWaste 的資料集處理和 YOLOv11n 的 fine-tune。firmware repo 負責韌體感測——L515 Depth 距離感測、LED 燈號控制、透過 Queue 送 user_detected。display repo 負責顯示與互動——狀態機、Roast 語音、Admin 面板、公告螢幕、事件紀錄。

---

## Slide 12 — 風險與退路

我們有兩個主要風險。

第一，Jetson 的環境配置可能卡關。JetPack 和 TensorRT 的版本相容性問題不少。如果真的搞不定，退路是直接用筆電執行推論做 Demo。

第二，L515 深度鏡頭可能借不到。備案是用一般 USB Webcam 搭配 HC-SR04 超音波感測器，替代 RGB 擷取與距離偵測功能。

---

## Slide 13 — 結尾

最後總結。

我們不是要羞辱使用者，而是把分類錯誤的瞬間，變成一次記得住的學習事件。

四個關鍵數字：Emoticon-bin 實證回收率可以提升 3 倍；我們目標辨識準確率 85% 以上；端到端延遲控制在 2.5 秒以內；使用者需要按的按鈕是 0 個——全自動。

謝謝聆聽，歡迎提問。
